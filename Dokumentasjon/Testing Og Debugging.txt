Jeg har ikke vscode installert, så jeg testet alle http endpointene med et automatisk python skript jeg lagde. Det tester forskjellig input, sender masse tull og ser hvordan serveren svarer. Det ser ut til at serveren er veldig glad i å kræsje når den får feilmeldinger fra SQL, så det har testingen kommet frem til. SQL serveren kræsjer ikke, bare selve node programmet. Dette kan løses med en try-catch blokk.